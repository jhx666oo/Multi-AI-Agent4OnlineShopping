{
  "name": "@shopping-agent/tool-gateway",
  "version": "0.1.0",
  "description": "Unified Tool Gateway with envelope, auth, idempotency, rate limiting, and audit",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint: skipped (no eslint configured)'",
    "test": "echo 'test: skipped (no tests yet)'"
  },
  "dependencies": {
    "@fastify/cors": "^8.4.0",
    "@fastify/helmet": "^11.1.0",
    "@fastify/rate-limit": "^9.1.0",
    "@opentelemetry/api": "^1.7.0",
    "@opentelemetry/exporter-trace-otlp-http": "^0.45.0",
    "@opentelemetry/sdk-node": "^0.45.0",
    "@shopping-agent/common": "workspace:*",
    "dotenv": "^16.3.0",
    "fastify": "^4.25.0",
    "fastify-plugin": "^5.1.0",
    "pino": "^8.17.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
